<!-- 🌌 Animated Contribution Banner (Takanashi Haryth - Purple Glow) -->
<svg xmlns="http://www.w3.org/2000/svg" viewBox="0 0 800 200" width="100%" height="200">
  <defs>
    <linearGradient id="grad" x1="0%" y1="0%" x2="100%" y2="100%">
      <stop offset="0%" stop-color="#a855f7"/>
      <stop offset="100%" stop-color="#7e22ce"/>
    </linearGradient>
    <filter id="glow">
      <feGaussianBlur stdDeviation="4" result="coloredBlur"/>
      <feMerge>
        <feMergeNode in="coloredBlur"/>
        <feMergeNode in="SourceGraphic"/>
      </feMerge>
    </filter>
  </defs>

  <!-- Background -->
  <rect width="800" height="200" fill="#0a0014"/>

  <!-- Animated Contribution Grid -->
  <g id="grid">
    <!-- Generate 70 squares -->
    <script type="text/ecmascript"><![CDATA[
      const svg = document.querySelector('#grid');
      let size = 12, gap = 5;
      for(let y=0;y<10;y++){
        for(let x=0;x<35;x++){
          let rect = document.createElementNS('http://www.w3.org/2000/svg','rect');
          rect.setAttribute('x', x*(size+gap)+40);
          rect.setAttribute('y', y*(size+gap)+40);
          rect.setAttribute('width', size);
          rect.setAttribute('height', size);
          rect.setAttribute('rx', 2);
          rect.setAttribute('fill', `hsl(${270 + Math.random()*20}, 80%, ${30+Math.random()*50}%)`);
          rect.style.animation = `pulse ${1+Math.random()*2}s infinite alternate`;
          svg.appendChild(rect);
        }
      }
    ]]></script>
  </g>

  <!-- Name Text -->
  <text x="50%" y="180" text-anchor="middle" font-size="28" font-family="'Segoe UI', sans-serif" fill="url(#grad)" filter="url(#glow)">
    Takanashi Haryth
  </text>

  <!-- CSS Animation -->
  <style><![CDATA[
    @keyframes pulse {
      from { opacity: 0.2; transform: scale(1); }
      to { opacity: 1; transform: scale(1.2); }
    }
  ]]></style>
</svg>
